<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>STNM</title>
    <link rel="stylesheet" href="{{ url_for("static", filename ="vendor/bootstrap.min.css") }}">
    <style type="text/css">
        .card {
            margin: 20px 0 40px 0;
        }

        .card table {
            margin-top: 20px;
            margin-bottom: 0;
        }
    </style>
</head>
<body>
<div id="root"></div>
<script src="{{ url_for("static", filename ="vendor/react.production.min.js") }}"></script>
<script src="{{ url_for("static", filename ="vendor/react-dom.production.min.js") }}"></script>
<script src="{{ url_for("static", filename ="vendor/react-router-dom.min.js") }}"></script>
<script src="{{ url_for("static", filename ="vendor/jquery.slim.min.js") }}"></script>
<script src="{{ url_for("static", filename ="vendor/bootstrap.bundle.min.js") }}"></script>
<script src="{{ url_for("static", filename ="vendor/babel.min.js") }}"></script>
<script type="text/babel">
    /*
   fetch("/api/status").then(r => r.json()).then(r => {
       // console.log(r)
   })
    */

    const {
        BrowserRouter: Router,
        Switch,
        Route,
        Link
    } = ReactRouterDOM;


    {% include "js/config-provider.jsx" %}
    {% include "js/node-info.jsx" %}
    {% include "js/panel.jsx" %}

    /*

    const App = () => {
        return (
            <ConfigProvider>
                <Home/>
            </ConfigProvider>
        );
    };

    const Home = () => {
        const {data, loadConfig} = React.useContext(ConfigContext);

        React.useEffect(() => {
            loadConfig();
        }, []);

        console.log(data)

        return <span>henlo</span>
    }


    ReactDOM.render(<App/>, document.getElementById("root"));
    */

    const App = () => {
        return <Router>
            <div className="container">
                <Switch>
                    <Route exact={true} strict={true} path="/ui">
                        <ConfigProvider>
                            <Panel/>
                        </ConfigProvider>
                    </Route>
                </Switch>
            </div>
        </Router>
    };

    ReactDOM.render(<App/>, document.getElementById("root"));
</script>
</body>
</html>